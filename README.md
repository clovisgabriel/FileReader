A C# console application that reads different types of files and supports optional encryption and role-based security.

The project was developed incrementally from version 1 to version 9 to demonstrate feature evolution.

## Supported file types
- Text
- XML
- JSON

## How to run

Run the FileReader.Cli project. The application will start automatically via Program.cs.
